To start server: run start-sv

Make sure NodeJS and nodemon are installed and on environment variables

The server uses port 80, so if there's some port conflicts make sure to close the apps using that port

To broadcast localhost online, use ngrok/proxylocal/pagekite. ngrok is free while others have to be used as trial